# Stash 收藏助手 — 隐私政策

**生效日期**：2026-07-18
**适用范围**：Chrome 扩展「Stash 收藏助手」（以下简称「本扩展」）

本政策说明本扩展如何收集、使用、存储与保护你的信息。使用本扩展即表示你同意以下条款。

---

## 1. 我们收集的信息

本扩展遵循**本地优先（local-first）**原则，仅在为你提供功能所必需时处理数据。

### 1.1 本地存储（默认，无需登录）
当你收藏网页或创建笔记时，以下信息会保存在你浏览器的 **IndexedDB** 本地数据库中：
- 收藏对象的 URL、标题、描述；
- 内容类型（仓库 / 文章 / 视频 / 商品 / 动态 / 图片 / 书签 / 笔记）；
- 你添加的标签、分类；
- 笔记正文；
- 代码仓库的 stars / forks、编程语言等公开元数据（来自代码托管平台的公开 API）；
- 收藏 / 更新时间。

这些数据**仅存储于你的设备**，不会自动上传，除非你主动开启云端同步。

### 1.2 云端同步（可选，需主动开启）
当你登录账号并开启「云端自动备份」后，上述收藏数据会通过 HTTPS 加密传输至我们的云服务（`https://hub.htui.cc`）以便多设备同步。云端仅存储功能所需的最小字段（与本地一致），**不会**额外记录你的浏览历史。

### 1.3 账号信息
使用云端功能时，你需要使用**手机号（mobile）**注册并登录，我们会收集你提供的**手机号**与**密码**（密码以哈希 / 单向加密形式存储，服务端不保留明文；手机号作为账号唯一标识）。我们**不收集**真实姓名、邮箱等强制个人信息；如你自愿补充，仅用于必要的账号与找回用途。

### 1.4 我们**不收集**
- 你不曾主动收藏的网页内容或浏览历史；
- 表单输入、键盘记录等任何与收藏无关的行为数据；
- 用于广告或跨站追踪的标识符。

---

## 2. 我们如何使用信息

- 在你的设备上展示、搜索、分类与管理你的收藏；
- 在你主动点击「刷新」时，向代码托管平台公开 API 请求仓库的 stars / forks 并展示；
- 在你开启云端同步时，备份与恢复你的收藏以实现多设备一致；
- 提供导入 / 导出功能，让你随时带走自己的数据。

我们**不会**将你的任何数据用于广告、画像或出售给第三方。

---

## 3. 数据共享与披露

我们仅在为实现本扩展功能所必需时，与以下接收方共享你的部分数据；上述接收方作为数据处理方，仅按功能所需处理你的数据，并受保密义务约束：

- **我们的云服务（`hub.htui.cc`）**：当你开启云端同步、登录 / 注册或使用人机验证时，收藏数据、账号信息（手机号、密码哈希）及验证参数会通过 HTTPS 传输并存储于该服务，用于多设备同步与账号鉴权。
- **代码托管平台（GitHub、Gitee、GitLab、Bitbucket、Codeberg、SourceForge 等）**：当你主动点击「刷新」时，本扩展会将对应仓库的 URL 发送至这些平台的**公开 API** 以读取 stars / forks 等公开元数据；这些信息本身不属于你的私密信息。
- 我们**不会**将你的个人数据出售、或用于广告、用户画像。
- 仅在法律要求，或为保护本扩展与用户安全所必需时，才可能在法律程序下披露信息。

除上文明确列出的接收方外，我们不会向任何第三方共享你的个人数据。

---

## 4. 数据存储与安全

- 本地数据存放于浏览器 IndexedDB，受浏览器沙箱保护；
- 云端传输使用 **HTTPS（TLS）** 加密；
- 密码以**哈希**形式存储，服务端不保留明文；
- 云服务访问需凭账号令牌（token），且遵循最小权限原则。
- 出于安全与防滥用需要，服务端可能记录访问时间、IP 地址等必要的技术日志，保留时间不超过达成安全目的所需期限。

### 4.1 数据保留期限
- **本地数据**：收藏数据一直保留在你的浏览器 IndexedDB 中，直至你主动清空或卸载扩展。
- **云端账号数据**：在你注销账号或主动发起删除请求后，对应的云端收藏与账号信息将被移除；我们不会在删除请求之外长期保留已注销账号的数据。

---

## 5. 你的权利与控制

你可以随时：
- **清空本地收藏**：在扩展「设置 → 数据管理 → 清空所有收藏」；
- **导出 / 导入**：将全部收藏导出为 JSON 文件备份或迁移；
- **关闭云端同步**：在「设置 → 数据同步」关闭自动备份；
- **删除云端数据**：退出账号后，云端对应收藏将被移除（你也可联系我们删除账号及全部数据）。

---

## 6. 权限用途说明

| 权限 | 用途 |
|------|------|
| `activeTab` / `tabs` | 在你主动收藏时读取当前页 URL / 标题 |
| `storage` | 保存主题、同步开关等设置 |
| `contextMenus` | 提供右键「收藏到 Stash」入口 |
| `notifications` | 收藏 / 同步结果的轻提示 |
| 代码平台 host 权限 | 仅在你点击「刷新」时获取仓库公开 stars/forks |
| `hub.htui.cc` host 权限 | 云端同步 API 地址 |

---

## 7. 儿童隐私

本扩展不面向 13 岁以下儿童，我们也不会故意收集其信息。

---

## 8. 政策变更

如本政策变更，我们会在扩展更新说明或官网公示。继续使用即视为接受更新后的政策。

---

## 9. 联系我们

如有隐私相关问题，可通过项目仓库或官网 `https://hub.htui.cc` 联系开发者。

---

*本隐私政策为通用模板，正式上架前请将其托管为可公开访问的网页（如 GitHub Pages 或 hub.htui.cc 静态页），并在 Chrome 开发者控制台填写该网址。*
